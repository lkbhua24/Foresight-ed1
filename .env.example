# Root environment example for local development

# Relayer backend port (only applied to relayer via ws:dev:all)
PORT=3001

# Frontend Supabase configuration
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-side Supabase service key (for /api/* write operations)
SUPABASE_SERVICE_KEY=

# Optional: direct DB connection (use only in infra scripts)
# SUPABASE_DB_URL=
# SUPABASE_CONNECTION_STRING=

# Notes:
# - ws:dev:all loads this file only for the relayer command, not for web.
# - Place sensitive keys in .env.local (ignored by VCS) to override defaults.
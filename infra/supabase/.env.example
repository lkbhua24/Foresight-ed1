# 仅供数据库管理脚本使用（不会被前端打包）
# 从 Supabase 控制台复制直连字符串：Project Settings → Database → Connection string → Node/psql

# 常用格式（任选其一）：
# postgresql://postgres.<project-ref>:<your-db-password>@aws-0-<region>.pooler.supabase.com:6543/postgres
# postgresql://postgres:<your-db-password>@db.<project-ref>.supabase.co:6543/postgres?sslmode=require

# 填写任意一个变量即可
SUPABASE_DB_URL=
# SUPABASE_CONNECTION_STRING=